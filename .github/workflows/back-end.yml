name: Weather api

on:
  push: 
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: build WeatherApi
        run: doccker build ./WeatherApi --file ./WeatherApi/Dockerfile --tag WeatherApi:$(date +$s)